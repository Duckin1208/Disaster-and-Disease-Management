<Window x:Class="Disease_Disaster.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập hệ thống" Height="480" Width="400" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

	<Border Background="White" CornerRadius="20" BorderBrush="#DCDDE1" BorderThickness="1">
		<Grid Margin="30">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>

			<StackPanel Grid.Row="0" Margin="0,10,0,20">
				<TextBlock Text="S.E.S" FontSize="32" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center"/>
				<TextBlock Text="Quản lý Dịch bệnh &amp; Thiên tai" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
			</StackPanel>

			<StackPanel Grid.Row="1">
				<TextBlock Text="Tên đăng nhập" FontWeight="SemiBold" Margin="0,0,0,5"/>
				<Border BorderBrush="#DCDDE1" BorderThickness="1" CornerRadius="5" Margin="0,0,0,15">
					<TextBox x:Name="txtUsername" Height="35" BorderThickness="0" Padding="10,0" VerticalContentAlignment="Center"/>
				</Border>

				<TextBlock Text="Mật khẩu" FontWeight="SemiBold" Margin="0,0,0,5"/>
				<Border BorderBrush="#DCDDE1" BorderThickness="1" CornerRadius="5">
					<PasswordBox x:Name="txtPassword" Height="35" BorderThickness="0" Padding="10,0" VerticalContentAlignment="Center"/>
				</Border>

				<Button Content="Quên mật khẩu? (1.26)" Foreground="#2980B9" Background="Transparent" 
                        BorderThickness="0" HorizontalAlignment="Right" Margin="0,5" Click="btnForgotPassword_Click"/>
			</StackPanel>

			<StackPanel Grid.Row="2">
				<Button x:Name="btnLogin" Content=" ĐĂNG NHẬP" Height="40" Background="#2C3E50" Foreground="White" 
                        FontWeight="Bold" Click="btnLogin_Click" Margin="0,0,0,10"/>

				<Button x:Name="btnRegister" Content="Chưa có tài khoản? Đăng ký ngay" Background="Transparent" 
                        BorderThickness="0" Foreground="#27AE60" FontWeight="SemiBold" Click="btnRegister_Click"/>

				<Button Content="Thoát" Margin="0,15,0,0" Background="Transparent" Foreground="#E74C3C" Click="btnClose_Click"/>
			</StackPanel>
		</Grid>
	</Border>
</Window>