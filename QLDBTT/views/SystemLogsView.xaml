<UserControl x:Class="Disease_Disaster.Views.SystemLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">
	<Grid Background="White" Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<Border Grid.Row="0" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10" Background="#F9F9F9" Margin="0,0,0,10">
			<StackPanel Orientation="Horizontal">
				<Label Content="Từ ngày:" VerticalAlignment="Center"/>
				<DatePicker x:Name="dpFrom" Width="120" Margin="5,0"/>

				<Label Content="Đến ngày:" VerticalAlignment="Center" Margin="10,0,0,0"/>
				<DatePicker x:Name="dpTo" Width="120" Margin="5,0"/>

				<Label Content="Tài khoản:" VerticalAlignment="Center" Margin="10,0,0,0"/>
				<TextBox x:Name="txtUser" Width="150" Height="25" VerticalContentAlignment="Center"/>

				<Button Content="Xem Nhật Ký" Width="100" Height="30" Margin="15,0,0,0" 
                        Background="#3498DB" Foreground="White" Click="btnFilter_Click"/>
			</StackPanel>
		</Border>

		<DataGrid x:Name="dgLogs" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                  BorderBrush="#CCCCCC" BorderThickness="1">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Thời Gian" Binding="{Binding ThoiGian, StringFormat='dd/MM/yyyy HH:mm'}" Width="140"/>
				<DataGridTextColumn Header="Tài Khoản" Binding="{Binding TaiKhoan}" Width="120" FontWeight="Bold"/>
				<DataGridTextColumn Header="Họ Tên" Binding="{Binding HoTen}" Width="150"/>
				<DataGridTextColumn Header="Hành Động" Binding="{Binding HanhDong}" Width="150" FontWeight="SemiBold"/>
				<DataGridTextColumn Header="Chi Tiết Thay Đổi" Binding="{Binding NoiDung}" Width="*"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>