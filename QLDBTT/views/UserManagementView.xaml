<UserControl x:Class="Disease_Disaster.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" Background="White">

	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<Border Grid.Row="0" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10" Background="#F9F9F9" Margin="0,0,0,10">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<StackPanel Orientation="Horizontal" Grid.Column="0">
					<Label Content="Tìm kiếm:" VerticalAlignment="Center"/>
					<TextBox x:Name="txtSearch" Width="250" Height="30" VerticalContentAlignment="Center" 
                             Margin="5,0" ToolTip="Nhập Tên, Tài khoản hoặc Email..."
                             TextChanged="txtSearch_TextChanged"/>
					<Button Content="Tìm" Width="60" Height="30" Click="btnSearch_Click" Margin="5,0,0,0"/>
				</StackPanel>

				<StackPanel x:Name="pnlAdminActions" Orientation="Horizontal" Grid.Column="2">
					<Button x:Name="btnAdd" Content="+ Thêm Mới" Width="100" Height="30" 
                            Background="#2ECC71" Foreground="White" FontWeight="Bold"
                            Click="btnAdd_Click"/>
				</StackPanel>
			</Grid>
		</Border>

		<DataGrid x:Name="dgUsers" Grid.Row="1" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" 
                  BorderBrush="#CCCCCC" BorderThickness="1" RowHeight="35">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Tài Khoản" Binding="{Binding TenDangNhap}" Width="150" FontWeight="Bold"/>
				<DataGridTextColumn Header="Họ và Tên" Binding="{Binding HoTen}" Width="200"/>
				<DataGridTextColumn Header="Vai Trò" Binding="{Binding TenQuyen}" Width="150"/>
				<DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
				<DataGridTextColumn Header="Số ĐT" Binding="{Binding SDT}" Width="120"/>

				<DataGridTemplateColumn Header="Thao tác" Width="120">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<Button x:Name="btnEdit" Content="Sửa" Width="50" Margin="0,0,5,0"
                        Background="#2980B9" Foreground="White" Click="btnEdit_Click"/>

								<Button x:Name="btnDelete" Content="Xóa" Width="50" 
                        Background="#E74C3C" Foreground="White" Click="btnDelete_Click"/>
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>